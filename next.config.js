/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "cdn3d.iconscout.com",
            "intaadvising.gatech.edu"
        ]
    }
}

module.exports = nextConfig
