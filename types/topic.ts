export type Topic = {
    id: number;
    name: string;
};
export type TopicType = {
    id: number;
    name: string;
    description: string;
};
export type CreateTopic = {
    name: string;
    description: string;
};
