export type Student = {
    email: string;
    fullName: string;
    avatar: string;
};

export type StudentType = {
    id: number;
    name: string;
    email: string;
    combination: string;
    userStatus: string;
    createdAt: string;
};
