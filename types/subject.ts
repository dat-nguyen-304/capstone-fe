export type Subject = {
    id: string;
    label: string;
    value: string;
    img: string;
};

export type Combination = {
    id: string;
    label: string;
    subjects: string[];
    value: string;
}
